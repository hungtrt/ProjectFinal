import { createSlice } from "@reduxjs/toolkit";
const salesReducer = createSlice({
  name: "sales",
  initialState: [

  ],
  reducers: {
   
  },
  
});
export const {  } = salesReducer.actions;
export default salesReducer.reducer;
